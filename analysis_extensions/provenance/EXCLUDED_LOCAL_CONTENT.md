# Excluded local content

This public candidate intentionally excludes patient/stay-level files, restricted raw or controlled inputs, real-data bootstrap replicates, manuscript/submission files, local configuration, logs, and absolute-path manifests.

The complete reviewer-oriented data package remains a separate local artifact and is not a GitHub release.
